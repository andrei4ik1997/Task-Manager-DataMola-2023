export enum Status {
  ToDo = 'To Do',
  InProgress = 'In progress',
  Complete = 'Complete',
}

export enum StatusParams {
  All,
  ToDo,
  InProgress,
  Complete,
}

export enum Priority {
  High = 'High',
  Medium = 'Medium',
  Low = 'Low',
}
